= simple_form_for [:admin, @product] do |f|

  = f.input :name, as: :string
  = f.input :description
  = f.input :price
  - if false
    = f.input :archive_index
  = f.input :image
  .actions
    = f.button :submit
