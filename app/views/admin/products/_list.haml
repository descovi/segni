%table.table.table-striped
  %thead
    %tr
      %th 
      %th Name
      %th Price
      %th Price label
      %th First
      %th
        .pull-right
          = link_to 'Add product', new_admin_product_path, class: 'btn btn-primary btn-xs'

  %tbody
    - products.each do |product|
      %tr
        %td
          - if product.image.present?
            = image_tag product.image.url(:_20x20c)
          - else
            n/a
        %td= product.name
        %td= product.price
        %td= product.price_label
        %td= product.first_page
        %td
          .pull-right
            = link_to 'Edit', edit_admin_product_path(product), class: 'btn btn-xs btn-link'
            = link_to 'Destroy', [:admin, product], :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-link btn-xs'