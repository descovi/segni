= simple_form_for([:great_admin, @website]) do |f|

  .form-inputs
    = f.input :name
    = f.input :name_slug
    = f.input :primary_color
    = f.input :secondary_color
    = f.input :user

  .form-actions
    = f.button :submit
